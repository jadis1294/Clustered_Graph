/**
 * This function for the object Underlying_graph.
 * @param {label} the label of the graph
 * @param {embedded} boolean value to decide if the rotationScheme of the node should be read
 * @param {clusters} array of the cluster in the graph
 * @param {nodes} array of the nodes in the graph
 * @param {edges} array of the edges in the graph
 */

function underlying_graph(label,embedded,clusters,nodes,edges) = {
    this.label= label,
    this.embedded= embedded,   //se è embedded il rotationScheme va letto, altrimenti no
    this.clusters=clusters,
    this.nodes= nodes,
    this.edges=edges,

}          

/**
 * This function for the object inclusion_tree.
 * @param {label} the label of the graph
 * @param {embedded} boolean value to decide if the rotationScheme of the node should be read
 * @param {clusters} array of the cluster in the graph
 * @param {nodes} array of the nodes in the graph
 * @param {edges} array of the edges in the graph
 */
function inclusion_tree()={
    
}       